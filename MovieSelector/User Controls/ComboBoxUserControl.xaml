<UserControl x:Class="ComboBoxUserControl.CustomComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MaxHeight="200"
             Background="#4a4a4a"
             FontSize="24"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border"
                                Background="Transparent"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="Transparent" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ListBoxItem}">
            <Setter Property="Margin" Value="0" />
            <Setter Property="Height" Value="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ActualHeight}" />
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=FontSize}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border Name="myBorder" Background="Transparent">
                            <ContentPresenter Margin="15,0,0,0"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center" />
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="myBorder" Property="Background" Value="#485560" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="myBorder" Property="Background" Value="#2c353e" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Rectangle Fill="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Background}" Stretch="Fill" />
        <Button MinWidth="0"
                MinHeight="0"
                Padding="0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch"
                Background="Transparent"
                BorderThickness="0"
                Click="ToggleButton_Click"
                Style="{StaticResource BaseButtonStyle}">
            <Grid Margin="15,5,5,5">
                <TextBlock Margin="0,0,0,2"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           FontSize="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=FontSize}"
                           FontWeight="Normal"
                           Foreground="White"
                           Text="{Binding Path=SelectedItem, ElementName=listBox}" />

                <Path Margin="0,6,6,0"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Top"
                      Data="F1 M 0.000,3.929 L 3.930,0.000 L 7.859,3.929 L 0.000,3.929 Z"
                      Fill="#ffcfcfcf" />

                <Path Margin="0,0,6,6"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Bottom"
                      Data="F1 M 0.000,3.929 L 3.930,0.000 L 7.859,3.929 L 0.000,3.929 Z"
                      Fill="#ffcfcfcf"
                      RenderTransformOrigin="0.5, 0.5">
                    <Path.RenderTransform>
                        <RotateTransform Angle="180" />
                    </Path.RenderTransform>
                </Path>
            </Grid>
        </Button>

        <Popup Name="listBoxPopUp"
               Width="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ActualWidth}"
               Height="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=MaxHeight}">
            <Grid>
                <ListBox Name="listBox"
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Stretch"
                         Background="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Background}"
                         BorderThickness="0"
                         PreviewMouseWheel="ListBox_PreviewMouseWheel"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Disabled"
                         SelectionChanged="listBox_SelectionChanged" />
                <Border Width="45"
                        BorderBrush="White"
                        BorderThickness="0,1,0,0" />
            </Grid>
        </Popup>
    </Grid>
</UserControl>
